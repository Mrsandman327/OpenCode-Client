// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * App 是 Wails 应用的核心结构体，所有绑定到前端的方法都定义在此。
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "/wails/runtime.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as json$0 from "../encoding/json/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as model$0 from "./model/models.js";

/**
 * AddModelType 添加模型配置类型分组。
 * @param {string} entryType
 * @returns {$CancellablePromise<model$0.SaveResult>}
 */
export function AddModelType(entryType) {
    return $Call.ByID(162494865, entryType).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * AddSkillSourceDir 添加技能源目录到配置。
 * 通过 a.sm.SourceDir() 获取 opencode 全局技能目录路径用于排除检查。
 * @param {string} dir
 * @returns {$CancellablePromise<model$0.SaveResult>}
 */
export function AddSkillSourceDir(dir) {
    return $Call.ByID(1574206835, dir).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * AnswerQuestion 回答 question 工具调用（answers 为按问题顺序的二维数组）。
 * @param {string} sessionID
 * @param {string[][]} answers
 * @returns {$CancellablePromise<model$0.APIResult>}
 */
export function AnswerQuestion(sessionID, answers) {
    return $Call.ByID(910414563, sessionID, answers).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType1($result);
    }));
}

/**
 * AppCall 为前端 Web 统一分发调用。
 * @param {string} method
 * @param {json$0.RawMessage[]} args
 * @returns {$CancellablePromise<any>}
 */
export function AppCall(method, args) {
    return $Call.ByID(786695140, method, args);
}

/**
 * ApplySkillScheme 应用指定名称的技能方案。
 * @param {string} name
 * @returns {$CancellablePromise<model$0.SchemeApplyResult>}
 */
export function ApplySkillScheme(name) {
    return $Call.ByID(1967576903, name).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType2($result);
    }));
}

/**
 * CheckOpenCodeVersion 检测 opencode 最新版本。
 * @param {string} currentVersion
 * @returns {$CancellablePromise<model$0.VersionCheckResult>}
 */
export function CheckOpenCodeVersion(currentVersion) {
    return $Call.ByID(2498031174, currentVersion).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType3($result);
    }));
}

/**
 * CreateBrowserDir 在当前文件浏览器目录下创建文件夹。
 * @param {string} rootDir
 * @param {string} path
 * @param {string} dirName
 * @returns {$CancellablePromise<model$0.SaveResult>}
 */
export function CreateBrowserDir(rootDir, path, dirName) {
    return $Call.ByID(2846340622, rootDir, path, dirName).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * CreateProjectEntry 在项目配置目录下创建新文件。
 * @param {string} rootDir
 * @param {string} category
 * @param {string} name
 * @returns {$CancellablePromise<model$0.ProjectConfigFileEntry>}
 */
export function CreateProjectEntry(rootDir, category, name) {
    return $Call.ByID(4067036230, rootDir, category, name).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType4($result);
    }));
}

/**
 * DeleteBrowserEntry 删除文件浏览器中的文件或目录。
 * @param {string} rootDir
 * @param {string} path
 * @returns {$CancellablePromise<model$0.SaveResult>}
 */
export function DeleteBrowserEntry(rootDir, path) {
    return $Call.ByID(1505422302, rootDir, path).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * DeleteModelType 删除整个模型配置类型分组。
 * @param {string} entryType
 * @returns {$CancellablePromise<model$0.SaveResult>}
 */
export function DeleteModelType(entryType) {
    return $Call.ByID(4233224839, entryType).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * DeleteProjectEntry 删除项目配置目录下的文件或空目录。
 * @param {string} rootDir
 * @param {string} category
 * @param {string} relPath
 * @returns {$CancellablePromise<void>}
 */
export function DeleteProjectEntry(rootDir, category, relPath) {
    return $Call.ByID(790991933, rootDir, category, relPath);
}

/**
 * DeleteProvider 删除供应商。
 * @param {string} key
 * @returns {$CancellablePromise<model$0.SaveResult>}
 */
export function DeleteProvider(key) {
    return $Call.ByID(3297626303, key).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * DeleteSkillScheme 删除指定技能方案。
 * @param {string} name
 * @returns {$CancellablePromise<model$0.SaveResult>}
 */
export function DeleteSkillScheme(name) {
    return $Call.ByID(2495426856, name).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * DiscardFile 撤销文件变更。
 * @param {string} rootDir
 * @param {string} path
 * @returns {$CancellablePromise<model$0.GitActionResult>}
 */
export function DiscardFile(rootDir, path) {
    return $Call.ByID(2822748111, rootDir, path).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType5($result);
    }));
}

/**
 * ExportConfigEntries 将结构化模型条目导出为 omo.jsonc 兼容文件。
 * @param {string} dir
 * @param {string} filename
 * @param {model$0.ModelEntry[]} entries
 * @returns {$CancellablePromise<string>}
 */
export function ExportConfigEntries(dir, filename, entries) {
    return $Call.ByID(1117269151, dir, filename, entries);
}

/**
 * GetAgentDescriptions 返回 agent/category 描述表。
 * @returns {$CancellablePromise<{ [_ in string]?: string }>}
 */
export function GetAgentDescriptions() {
    return $Call.ByID(2735655447).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType6($result);
    }));
}

/**
 * GetAggregatedSkills 返回所有技能的聚合列表：
 * 1. 从配置读取来源目录
 * 2. 扫描所有来源目录
 * 3. 扫描全局目录
 * 4. 合并返回完整列表
 * 当没有配置来源目录时，回退到原有的 GetAllSkills 扫描逻辑。
 * @returns {$CancellablePromise<model$0.SkillInfo[]>}
 */
export function GetAggregatedSkills() {
    return $Call.ByID(1612910348).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType8($result);
    }));
}

/**
 * GetCommands 返回所有常用命令分组数据。
 * @returns {$CancellablePromise<model$0.CmdGroup[]>}
 */
export function GetCommands() {
    return $Call.ByID(983453965).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType10($result);
    }));
}

/**
 * GetConfigPath 返回模型配置文件路径。
 * @returns {$CancellablePromise<string>}
 */
export function GetConfigPath() {
    return $Call.ByID(4029735838);
}

/**
 * GetDirEnabledSkills 返回指定来源目录中当前已启用的技能名称列表。
 * 前端删除目录前调用此接口以展示受影响技能列表供用户确认。
 * @param {string} dir
 * @returns {$CancellablePromise<string[]>}
 */
export function GetDirEnabledSkills(dir) {
    return $Call.ByID(3749324257, dir).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType11($result);
    }));
}

/**
 * GetFrontendWebStatus 返回页面访问服务状态。
 * @param {string} hostname
 * @param {number} port
 * @returns {$CancellablePromise<model$0.WebResult>}
 */
export function GetFrontendWebStatus(hostname, port) {
    return $Call.ByID(3701122253, hostname, port).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType12($result);
    }));
}

/**
 * GetGitHistory 返回目录提交历史列表。
 * @param {string} rootDir
 * @param {number} offset
 * @param {number} limit
 * @returns {$CancellablePromise<model$0.GitHistoryResult>}
 */
export function GetGitHistory(rootDir, offset, limit) {
    return $Call.ByID(1604680175, rootDir, offset, limit).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType13($result);
    }));
}

/**
 * GetGitHistoryFiles 返回指定提交的文件列表。
 * @param {string} rootDir
 * @param {string} commitHash
 * @returns {$CancellablePromise<model$0.GitCommitFilesResult>}
 */
export function GetGitHistoryFiles(rootDir, commitHash) {
    return $Call.ByID(2965185174, rootDir, commitHash).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType14($result);
    }));
}

/**
 * GetGitHistoryPreview 返回指定提交中文件的 diff 预览。
 * @param {string} rootDir
 * @param {string} commitHash
 * @param {string} path
 * @returns {$CancellablePromise<model$0.GitCommitFilePreviewResult>}
 */
export function GetGitHistoryPreview(rootDir, commitHash, path) {
    return $Call.ByID(3997727865, rootDir, commitHash, path).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType15($result);
    }));
}

/**
 * GetGitPreview 返回单文件 Git 预览结果。
 * @param {string} rootDir
 * @param {string} path
 * @returns {$CancellablePromise<model$0.GitFilePreviewResult>}
 */
export function GetGitPreview(rootDir, path) {
    return $Call.ByID(3217409447, rootDir, path).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType16($result);
    }));
}

/**
 * GetGitStatus 返回目录 Git 变更状态。
 * @param {string} rootDir
 * @returns {$CancellablePromise<model$0.GitStatusResult>}
 */
export function GetGitStatus(rootDir) {
    return $Call.ByID(4123560639, rootDir).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType17($result);
    }));
}

/**
 * GetGlobalOpenCodeConfig 返回全局 opencode 配置文件的路径和内容。
 * @returns {$CancellablePromise<model$0.GlobalConfigInfo>}
 */
export function GetGlobalOpenCodeConfig() {
    return $Call.ByID(240158979).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType18($result);
    }));
}

/**
 * GetImportableSkills 返回可导入到项目中的技能列表。
 * @param {string} rootDir
 * @returns {$CancellablePromise<model$0.ImportableSkill[]>}
 */
export function GetImportableSkills(rootDir) {
    return $Call.ByID(1791845888, rootDir).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType20($result);
    }));
}

/**
 * GetModelConfig 读取所有 agent/category 的模型配置。
 * @returns {$CancellablePromise<model$0.ModelEntry[]>}
 */
export function GetModelConfig() {
    return $Call.ByID(1396865434).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType22($result);
    }));
}

/**
 * GetModelList 调用供应商 API 获取可用模型列表。
 * @param {string} baseURL
 * @param {string} apiKey
 * @returns {$CancellablePromise<string[]>}
 */
export function GetModelList(baseURL, apiKey) {
    return $Call.ByID(4177555444, baseURL, apiKey).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType11($result);
    }));
}

/**
 * GetProjectConfigSummary 返回项目 .opencode/ 配置的四个 tab 聚合信息。
 * @param {string} rootDir
 * @returns {$CancellablePromise<model$0.ProjectConfigSummary>}
 */
export function GetProjectConfigSummary(rootDir) {
    return $Call.ByID(3184254470, rootDir).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType23($result);
    }));
}

/**
 * GetProjectTree 获取项目→目录→会话的树形结构 JSON。
 * @param {string} knownDirs
 * @returns {$CancellablePromise<string>}
 */
export function GetProjectTree(knownDirs) {
    return $Call.ByID(1878567804, knownDirs);
}

/**
 * GetProviderConfigPath 返回供应商配置文件路径。
 * @returns {$CancellablePromise<string>}
 */
export function GetProviderConfigPath() {
    return $Call.ByID(2693012447);
}

/**
 * GetProviders 获取所有供应商配置。
 * @returns {$CancellablePromise<model$0.ProviderInfo[]>}
 */
export function GetProviders() {
    return $Call.ByID(2525123639).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType25($result);
    }));
}

/**
 * GetSchemeDir 返回方案目录的绝对路径。
 * @returns {$CancellablePromise<string>}
 */
export function GetSchemeDir() {
    return $Call.ByID(1688609619);
}

/**
 * GetSkillConfig 返回技能管理页面需要的完整聚合数据（统一接口）。
 * @returns {$CancellablePromise<model$0.SkillConfigResult>}
 */
export function GetSkillConfig() {
    return $Call.ByID(1940673122).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType26($result);
    }));
}

/**
 * GetSkillSourceDirs 返回当前配置中所有技能源目录。
 * @returns {$CancellablePromise<string[]>}
 */
export function GetSkillSourceDirs() {
    return $Call.ByID(155479609).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType11($result);
    }));
}

/**
 * GetSkills 返回所有技能及其在各平台的链接状态。
 * @returns {$CancellablePromise<model$0.SkillInfo[]>}
 */
export function GetSkills() {
    return $Call.ByID(1520668053).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType8($result);
    }));
}

/**
 * GetSourceDir 返回技能源目录路径。
 * @returns {$CancellablePromise<string>}
 */
export function GetSourceDir() {
    return $Call.ByID(25163249);
}

/**
 * GetStats 返回统计信息。
 * @returns {$CancellablePromise<model$0.Stats>}
 */
export function GetStats() {
    return $Call.ByID(2859423874).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType27($result);
    }));
}

/**
 * GetWebStatus 返回当前 web 服务状态。
 * @param {string} hostname
 * @param {number} port
 * @returns {$CancellablePromise<model$0.WebResult>}
 */
export function GetWebStatus(hostname, port) {
    return $Call.ByID(1108731257, hostname, port).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType12($result);
    }));
}

/**
 * GitCommit 提交当前暂存区。
 * @param {string} rootDir
 * @param {string} message
 * @returns {$CancellablePromise<model$0.GitActionResult>}
 */
export function GitCommit(rootDir, message) {
    return $Call.ByID(1971060042, rootDir, message).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType5($result);
    }));
}

/**
 * GitPull 从远端拉取当前分支。
 * @param {string} rootDir
 * @param {model$0.ProxyConfig} proxy
 * @returns {$CancellablePromise<model$0.GitActionResult>}
 */
export function GitPull(rootDir, proxy) {
    return $Call.ByID(3933172764, rootDir, proxy).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType5($result);
    }));
}

/**
 * GitPush 推送当前分支到远端。
 * @param {string} rootDir
 * @param {model$0.ProxyConfig} proxy
 * @returns {$CancellablePromise<model$0.GitActionResult>}
 */
export function GitPush(rootDir, proxy) {
    return $Call.ByID(4036251239, rootDir, proxy).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType5($result);
    }));
}

/**
 * ImportSkill 将技能通过软链接导入到项目 .opencode/skills/ 中。
 * @param {string} rootDir
 * @param {string} sourcePath
 * @param {string} skillName
 * @returns {$CancellablePromise<void>}
 */
export function ImportSkill(rootDir, sourcePath, skillName) {
    return $Call.ByID(631817921, rootDir, sourcePath, skillName);
}

/**
 * KnowledgeCategories 返回完整知识库分类树。
 * @returns {$CancellablePromise<model$0.KnowledgeCategory[]>}
 */
export function KnowledgeCategories() {
    return $Call.ByID(3574015327).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType29($result);
    }));
}

/**
 * KnowledgeConvert 执行知识库条目转化，返回写入目标的完整路径。
 * @param {model$0.ConvertRequest} req
 * @returns {$CancellablePromise<string>}
 */
export function KnowledgeConvert(req) {
    return $Call.ByID(1278934526, req);
}

/**
 * KnowledgeConvertPreview 预览知识库条目转化为 OpenCode 资产的结果（不产生任何写入）。
 * @param {model$0.ConvertRequest} req
 * @returns {$CancellablePromise<model$0.ConvertPreview | null>}
 */
export function KnowledgeConvertPreview(req) {
    return $Call.ByID(2535064914, req).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType31($result);
    }));
}

/**
 * KnowledgeDelete 删除知识库条目。
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function KnowledgeDelete(id) {
    return $Call.ByID(3501328832, id);
}

/**
 * KnowledgeGet 返回单个知识库条目（含正文）。
 * @param {string} id
 * @returns {$CancellablePromise<model$0.KnowledgeEntry | null>}
 */
export function KnowledgeGet(id) {
    return $Call.ByID(54334893, id).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType33($result);
    }));
}

/**
 * KnowledgeList 返回知识库条目元数据列表（不含正文）。
 * @returns {$CancellablePromise<model$0.KnowledgeEntry[]>}
 */
export function KnowledgeList() {
    return $Call.ByID(4043013547).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType34($result);
    }));
}

/**
 * KnowledgeSave 新建或更新知识库条目，返回最终条目 ID。
 * @param {model$0.KnowledgeEntry} entry
 * @returns {$CancellablePromise<string>}
 */
export function KnowledgeSave(entry) {
    return $Call.ByID(2781533502, entry);
}

/**
 * KnowledgeSaveCategories 整树覆盖写入知识库分类。
 * @param {model$0.KnowledgeCategory[]} cats
 * @returns {$CancellablePromise<void>}
 */
export function KnowledgeSaveCategories(cats) {
    return $Call.ByID(3417203294, cats);
}

/**
 * LaunchWindowsTerminal 在外部终端中打开 opencode。
 * @param {string} mode
 * @param {string} webURL
 * @param {string} dir
 * @returns {$CancellablePromise<model$0.WebResult>}
 */
export function LaunchWindowsTerminal(mode, webURL, dir) {
    return $Call.ByID(2591073375, mode, webURL, dir).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType12($result);
    }));
}

/**
 * ListBrowsableDirs 返回目录浏览器当前层的目录列表。
 * @param {string} path
 * @returns {$CancellablePromise<model$0.DirectoryEntry[]>}
 */
export function ListBrowsableDirs(path) {
    return $Call.ByID(1659078832, path).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType36($result);
    }));
}

/**
 * ListBrowserFiles 返回站内文件浏览器目录列表。
 * @param {string} rootDir
 * @param {string} path
 * @returns {$CancellablePromise<model$0.FileBrowserListResult>}
 */
export function ListBrowserFiles(rootDir, path) {
    return $Call.ByID(4238510186, rootDir, path).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType37($result);
    }));
}

/**
 * ListProjectConfigDir 列出项目配置目录下的文件列表。
 * @param {string} rootDir
 * @param {string} category
 * @param {string} relPath
 * @returns {$CancellablePromise<model$0.ProjectConfigTab>}
 */
export function ListProjectConfigDir(rootDir, category, relPath) {
    return $Call.ByID(1219602019, rootDir, category, relPath).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType38($result);
    }));
}

/**
 * ListSchemes 扫描方案目录并返回所有方案文件信息。
 * @returns {$CancellablePromise<model$0.SchemeInfo[]>}
 */
export function ListSchemes() {
    return $Call.ByID(3224892887).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType40($result);
    }));
}

/**
 * ListSkillSchemes 返回所有技能方案名称列表。
 * @returns {$CancellablePromise<string[]>}
 */
export function ListSkillSchemes() {
    return $Call.ByID(903421030).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType11($result);
    }));
}

/**
 * OpenCodeAPI 代理访问本机 opencode serve API。
 * @param {string} method
 * @param {string} path
 * @param {string} body
 * @returns {$CancellablePromise<model$0.APIResult>}
 */
export function OpenCodeAPI(method, path, body) {
    return $Call.ByID(4008055560, method, path, body).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType1($result);
    }));
}

/**
 * OpenDir 在文件资源管理器中打开指定目录。
 * @param {string} path
 * @returns {$CancellablePromise<void>}
 */
export function OpenDir(path) {
    return $Call.ByID(288356608, path);
}

/**
 * OpenDirectoryDialog 打开目录选择对话框，返回所选目录路径（取消时返回空字符串）。
 * @returns {$CancellablePromise<string>}
 */
export function OpenDirectoryDialog() {
    return $Call.ByID(1188724780);
}

/**
 * OpenSchemeDir 在文件资源管理器中打开方案目录。
 * @returns {$CancellablePromise<void>}
 */
export function OpenSchemeDir() {
    return $Call.ByID(1378225629);
}

/**
 * OpenURL 用系统默认浏览器打开指定 URL（外部链接统一走这里，避免 WebView 导航离开工作台）。
 * @param {string} url
 * @returns {$CancellablePromise<void>}
 */
export function OpenURL(url) {
    return $Call.ByID(3584934946, url);
}

/**
 * ParseConfigContent 解析任意 JSONC 文本（导入场景），返回结构化模型条目。
 * @param {string} content
 * @returns {$CancellablePromise<model$0.ModelEntry[]>}
 */
export function ParseConfigContent(content) {
    return $Call.ByID(3729550523, content).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType22($result);
    }));
}

/**
 * ReadBrowserFile 返回站内文件浏览器文本文件内容。
 * @param {string} rootDir
 * @param {string} path
 * @returns {$CancellablePromise<model$0.FileBrowserReadResult>}
 */
export function ReadBrowserFile(rootDir, path) {
    return $Call.ByID(4044319337, rootDir, path).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType41($result);
    }));
}

/**
 * ReadBrowserRawBase64 返回原始文件 Base64 内容，供桌面端图片/PDF/下载使用。
 * @param {string} rootDir
 * @param {string} path
 * @returns {$CancellablePromise<model$0.FileBrowserRawResult>}
 */
export function ReadBrowserRawBase64(rootDir, path) {
    return $Call.ByID(1199709136, rootDir, path).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType42($result);
    }));
}

/**
 * ReadProjectConfigFile 读取项目配置文件内容。
 * @param {string} rootDir
 * @param {string} category
 * @param {string} relPath
 * @returns {$CancellablePromise<model$0.ProjectConfigFileResult>}
 */
export function ReadProjectConfigFile(rootDir, category, relPath) {
    return $Call.ByID(46517510, rootDir, category, relPath).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType43($result);
    }));
}

/**
 * ReadSchemeEntries 读取方案文件并解析为结构化模型条目列表。
 * @param {string} name
 * @returns {$CancellablePromise<model$0.ModelEntry[]>}
 */
export function ReadSchemeEntries(name) {
    return $Call.ByID(3434943730, name).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType22($result);
    }));
}

/**
 * Refresh 重新扫描技能目录并刷新状态。
 * @returns {$CancellablePromise<void>}
 */
export function Refresh() {
    return $Call.ByID(2576512028);
}

/**
 * RejectQuestion 忽略 question 工具调用。
 * @param {string} sessionID
 * @returns {$CancellablePromise<model$0.APIResult>}
 */
export function RejectQuestion(sessionID) {
    return $Call.ByID(4071089598, sessionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType1($result);
    }));
}

/**
 * RemoveSkillSourceDir 从配置中移除指定的技能源目录。
 * 同时会解除该目录下已启用的技能链接（清理托管链接）。
 * @param {string} dir
 * @returns {$CancellablePromise<model$0.SaveResult>}
 */
export function RemoveSkillSourceDir(dir) {
    return $Call.ByID(1546532666, dir).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * SaveBrowserFile 保存站内文件浏览器文本文件内容。
 * @param {string} rootDir
 * @param {string} path
 * @param {string} content
 * @returns {$CancellablePromise<model$0.SaveResult>}
 */
export function SaveBrowserFile(rootDir, path, content) {
    return $Call.ByID(3745120270, rootDir, path, content).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * SaveProjectConfigFile 写入项目配置文件。
 * @param {string} rootDir
 * @param {string} category
 * @param {string} relPath
 * @param {string} content
 * @returns {$CancellablePromise<model$0.ProjectConfigFileResult>}
 */
export function SaveProjectConfigFile(rootDir, category, relPath, content) {
    return $Call.ByID(1764389193, rootDir, category, relPath, content).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType43($result);
    }));
}

/**
 * SaveProvider 保存供应商配置。
 * @param {model$0.ProviderSave} ps
 * @returns {$CancellablePromise<model$0.SaveResult>}
 */
export function SaveProvider(ps) {
    return $Call.ByID(1885182733, ps).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * SaveSchemeEntries 将结构化模型条目保存为方案文件（后端生成 omo.jsonc 兼容结构）。
 * @param {string} name
 * @param {model$0.ModelEntry[]} entries
 * @returns {$CancellablePromise<void>}
 */
export function SaveSchemeEntries(name, entries) {
    return $Call.ByID(3982404781, name, entries);
}

/**
 * SaveSkillScheme 保存当前已启用的技能为方案。
 * 从聚合列表中筛选出 Linked=true 的技能，保存其名称列表。
 * @param {string} name
 * @returns {$CancellablePromise<model$0.SaveResult>}
 */
export function SaveSkillScheme(name) {
    return $Call.ByID(1652296650, name).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * StageAllFiles 暂存所有未暂存文件。
 * @param {string} rootDir
 * @returns {$CancellablePromise<model$0.GitActionResult>}
 */
export function StageAllFiles(rootDir) {
    return $Call.ByID(547348467, rootDir).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType5($result);
    }));
}

/**
 * StageFile 暂存指定文件。
 * @param {string} rootDir
 * @param {string} path
 * @returns {$CancellablePromise<model$0.GitActionResult>}
 */
export function StageFile(rootDir, path) {
    return $Call.ByID(3573038243, rootDir, path).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType5($result);
    }));
}

/**
 * StartFrontendWeb 启动页面访问服务。
 * @param {number} port
 * @param {string} hostname
 * @returns {$CancellablePromise<model$0.WebResult>}
 */
export function StartFrontendWeb(port, hostname) {
    return $Call.ByID(3112289795, port, hostname).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType12($result);
    }));
}

/**
 * StartOpenCodeEvents 连接 opencode 全局 SSE。
 * @returns {$CancellablePromise<model$0.APIResult>}
 */
export function StartOpenCodeEvents() {
    return $Call.ByID(3535731511).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType1($result);
    }));
}

/**
 * StartOpenCodeWeb 启动 opencode serve。
 * @param {number} port
 * @param {string} hostname
 * @param {model$0.ProxyConfig} proxy
 * @returns {$CancellablePromise<model$0.WebResult>}
 */
export function StartOpenCodeWeb(port, hostname, proxy) {
    return $Call.ByID(595414420, port, hostname, proxy).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType12($result);
    }));
}

/**
 * StatBrowserFile 返回站内文件浏览器文件信息。
 * @param {string} rootDir
 * @param {string} path
 * @returns {$CancellablePromise<model$0.FileBrowserStatResult>}
 */
export function StatBrowserFile(rootDir, path) {
    return $Call.ByID(311353955, rootDir, path).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType44($result);
    }));
}

/**
 * StopFrontendWeb 停止页面访问服务。
 * @returns {$CancellablePromise<model$0.WebResult>}
 */
export function StopFrontendWeb() {
    return $Call.ByID(2581302353).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType12($result);
    }));
}

/**
 * StopOpenCodeEvents 停止 SSE 转发。
 * @returns {$CancellablePromise<model$0.APIResult>}
 */
export function StopOpenCodeEvents() {
    return $Call.ByID(2369516441).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType1($result);
    }));
}

/**
 * StopOpenCodeWeb 停止 opencode web 服务。
 * @returns {$CancellablePromise<model$0.WebResult>}
 */
export function StopOpenCodeWeb() {
    return $Call.ByID(888045870).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType12($result);
    }));
}

/**
 * ToggleSkill 切换技能链接状态。
 * @param {string} skillPath
 * @param {string} skillName
 * @param {boolean} enable
 * @returns {$CancellablePromise<model$0.ToggleResult>}
 */
export function ToggleSkill(skillPath, skillName, enable) {
    return $Call.ByID(2106209646, skillPath, skillName, enable).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType45($result);
    }));
}

/**
 * UnstageFile 取消暂存指定文件。
 * @param {string} rootDir
 * @param {string} path
 * @returns {$CancellablePromise<model$0.GitActionResult>}
 */
export function UnstageFile(rootDir, path) {
    return $Call.ByID(3689349610, rootDir, path).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType5($result);
    }));
}

/**
 * UpdateModels 批量更新模型配置并保存到 JSONC 文件，同时将描述写入 agents-comments.json。
 * @param {model$0.ModelEntry[]} entries
 * @returns {$CancellablePromise<model$0.SaveResult>}
 */
export function UpdateModels(entries) {
    return $Call.ByID(1719430574, entries).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * UploadBrowserFile 上传单个文件到当前文件浏览器目录。
 * @param {string} rootDir
 * @param {string} path
 * @param {string} fileName
 * @param {string} base64Data
 * @param {boolean} overwrite
 * @returns {$CancellablePromise<model$0.FileBrowserUploadResult>}
 */
export function UploadBrowserFile(rootDir, path, fileName, base64Data, overwrite) {
    return $Call.ByID(301761818, rootDir, path, fileName, base64Data, overwrite).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType46($result);
    }));
}

// Private type creation functions
const $$createType0 = model$0.SaveResult.createFrom;
const $$createType1 = model$0.APIResult.createFrom;
const $$createType2 = model$0.SchemeApplyResult.createFrom;
const $$createType3 = model$0.VersionCheckResult.createFrom;
const $$createType4 = model$0.ProjectConfigFileEntry.createFrom;
const $$createType5 = model$0.GitActionResult.createFrom;
const $$createType6 = $Create.Map($Create.Any, $Create.Any);
const $$createType7 = model$0.SkillInfo.createFrom;
const $$createType8 = $Create.Array($$createType7);
const $$createType9 = model$0.CmdGroup.createFrom;
const $$createType10 = $Create.Array($$createType9);
const $$createType11 = $Create.Array($Create.Any);
const $$createType12 = model$0.WebResult.createFrom;
const $$createType13 = model$0.GitHistoryResult.createFrom;
const $$createType14 = model$0.GitCommitFilesResult.createFrom;
const $$createType15 = model$0.GitCommitFilePreviewResult.createFrom;
const $$createType16 = model$0.GitFilePreviewResult.createFrom;
const $$createType17 = model$0.GitStatusResult.createFrom;
const $$createType18 = model$0.GlobalConfigInfo.createFrom;
const $$createType19 = model$0.ImportableSkill.createFrom;
const $$createType20 = $Create.Array($$createType19);
const $$createType21 = model$0.ModelEntry.createFrom;
const $$createType22 = $Create.Array($$createType21);
const $$createType23 = model$0.ProjectConfigSummary.createFrom;
const $$createType24 = model$0.ProviderInfo.createFrom;
const $$createType25 = $Create.Array($$createType24);
const $$createType26 = model$0.SkillConfigResult.createFrom;
const $$createType27 = model$0.Stats.createFrom;
const $$createType28 = model$0.KnowledgeCategory.createFrom;
const $$createType29 = $Create.Array($$createType28);
const $$createType30 = model$0.ConvertPreview.createFrom;
const $$createType31 = $Create.Nullable($$createType30);
const $$createType32 = model$0.KnowledgeEntry.createFrom;
const $$createType33 = $Create.Nullable($$createType32);
const $$createType34 = $Create.Array($$createType32);
const $$createType35 = model$0.DirectoryEntry.createFrom;
const $$createType36 = $Create.Array($$createType35);
const $$createType37 = model$0.FileBrowserListResult.createFrom;
const $$createType38 = model$0.ProjectConfigTab.createFrom;
const $$createType39 = model$0.SchemeInfo.createFrom;
const $$createType40 = $Create.Array($$createType39);
const $$createType41 = model$0.FileBrowserReadResult.createFrom;
const $$createType42 = model$0.FileBrowserRawResult.createFrom;
const $$createType43 = model$0.ProjectConfigFileResult.createFrom;
const $$createType44 = model$0.FileBrowserStatResult.createFrom;
const $$createType45 = model$0.ToggleResult.createFrom;
const $$createType46 = model$0.FileBrowserUploadResult.createFrom;
